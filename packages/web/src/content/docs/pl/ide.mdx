---
title: IDE
description: Rozszerzenie obelisk dla VS Code, Cursor i innych IDE
---

obelisk integruje się z VS Code, Cursorem lub obsługującym terminal IDE. Aby uruchomić, po prostu uruchom `obelisk` w terminalu.

---

## Stosowanie

- **Szybkie uruchomienie**: użycie `Cmd+Esc` (Mac) lub `Ctrl+Esc` (Windows/Linux), aby uruchomić obelisk w uruchamianiu terminala lub skoncentruj się na uruchamianiu terminala, jeśli jest już uruchomiony.
- **Nowa sesja**: `Cmd+Shift+Esc` (Mac) lub `Ctrl+Shift+Esc` (Windows/Linux), aby została nowa konfiguracja terminala obelisk, nawet jeśli jest już otwarta. Możesz także kliknąć przycisk obelisk w interfejsie użytkownika.
- **Świadomość kontekstu**: Automatycznie udostępnia wybór lub kartę za pomocą obelisk.
- **Skróty do odwołania do plików**: `Cmd+Option+K` (Mac) lub `Alt+Ctrl+K` (Linux/Windows), aby wstawić odnośnik do plików. Na przykład `@File#L37-42`.

---

## Instalacja

Aby poznać obelisk na VS Code w forkach, takich jak Cursor, Windsurf, VSCodium:

1. Otwórz kod VS
2. Otwórz innowacyjny terminal
3. Uruchom `obelisk` — rozszerzenie instaluje się automatycznie

Jeśli z drugiej strony chcesz zainstalować własne IDE po uruchomieniu `/editor` lub `/export` z TUI, należy zainstalować `export EDITOR="code --wait"`. [Dowiedz się więcej](/docs/tui/#editor-setup).

---

### Instalacja ręczna

Wyszukaj **obelisk** na rynku rozszerzonym i kliknij **Zainstaluj**.

---

### Rozwiązywanie problemów

Jeśli rozwiązanie nie zostanie zainstalowane automatycznie:

- zastosowanie się, że użycie `obelisk` w terminalu.
- zadziałanie, że CLI dla Twojego IDE jest zainstalowane:
  - Dla kodu VS: polecenie `code`
  - Dla kura: polecenie `cursor`
  - Dla windsurfingu: decyzja `windsurf`
  - Dla VSCodium: decyzja `codium`
  - Jeśli nie, uruchom `Cmd+Shift+P` (Mac) lub `Ctrl+Shift+P` (Windows/Linux) i wyszukaj „Polecenie: zastosowanie decyzji„ kod ”w PATH” (lub właściwyk dla twojego IDE)
- zadziałanie, że VS Code ma pozwolenie na instalację rozszerzoną
