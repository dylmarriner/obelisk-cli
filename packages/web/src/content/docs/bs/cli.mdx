---
title: CLI
description: Obelisk CLI opcije i naredbe.
---

import { Tabs, TabItem } from "@astrojs/starlight/components"

Obelisk CLI po defaultu pokreće [TUI](/docs/tui) kada se pokrene bez ikakvih argumenata.

```bash
obelisk
```

Ali takođe prihvata naredbe kao što je dokumentovano na ovoj stranici. Ovo vam omogućava programsku interakciju sa Obelisk.

```bash
obelisk run "Explain how closures work in JavaScript"
```

---

### tui

Pokrenite Obelisk terminalski korisnički interfejs.

```bash
obelisk [project]
```

#### Opcije

| Opcija                                   | Kratko | Opis                                                                     |
| ---------------------------------------- | ------ | ------------------------------------------------------------------------ |
| <nobr><code>{"--continue"}</code></nobr> | `-c`   | Nastavite posljednju sesiju                                              |
| <nobr><code>{"--session"}</code></nobr>  | `-s`   | ID sesije za nastavak                                                    |
| <nobr><code>{"--fork"}</code></nobr>     |        | Forkujte sesiju pri nastavku (koristiti sa `--continue` ili `--session`) |
| <nobr><code>{"--prompt"}</code></nobr>   |        | Prompt za upotrebu                                                       |
| <nobr><code>{"--model"}</code></nobr>    | `-m`   | Model za korištenje u obliku provider/model                              |
| <nobr><code>{"--agent"}</code></nobr>    |        | Agent za korištenje                                                      |
| <nobr><code>{"--port"}</code></nobr>     |        | Port na kojem treba slušati                                              |
| <nobr><code>{"--hostname"}</code></nobr> |        | Hostname na kojem treba slušati                                          |

---

## Naredbe

Obelisk CLI takođe ima sljedeće naredbe.

---

### agent

Upravljajte Obelisk agentima.

```bash
obelisk agent [command]
```

---

### attach

Priključite terminal na već pokrenut Obelisk backend server pokrenut putem `serve` ili `web` naredbi.

```bash
obelisk attach [url]
```

Ovo omogućava korištenje TUI-ja sa udaljenim Obelisk backend-om. Na primjer:

```bash
# Start the backend server for web/mobile access
obelisk web --port 4096 --hostname 0.0.0.0

# In another terminal, attach the TUI to the running backend
obelisk attach http://10.20.30.40:4096
```

#### Opcije

| Opcija                                   | Kratko | Opis                                                                                          |
| ---------------------------------------- | ------ | --------------------------------------------------------------------------------------------- |
| <nobr><code>{"--dir"}</code></nobr>      |        | Radni direktorij za pokretanje TUI-a                                                          |
| <nobr><code>{"--continue"}</code></nobr> | `-c`   | Nastavi posljednju sesiju                                                                     |
| <nobr><code>{"--session"}</code></nobr>  | `-s`   | ID sesije za nastavak                                                                         |
| <nobr><code>{"--fork"}</code></nobr>     |        | Forkuj sesiju prilikom nastavka (koristite sa `--continue` ili `--session`)                   |
| <nobr><code>{"--password"}</code></nobr> | `-p`   | Lozinka za osnovnu autentifikaciju (zadano: `OBELISK_SERVER_PASSWORD`)                       |
| <nobr><code>{"--username"}</code></nobr> | `-u`   | Korisničko ime za osnovnu autentifikaciju (zadano: `OBELISK_SERVER_USERNAME` ili `obelisk`) |

---

#### create

Kreirajte novog agenta s prilagođenom konfiguracijom.

```bash
obelisk agent create
```

Ova naredba će vas voditi kroz kreiranje novog agenta sa prilagođenim sistemskim promptom i konfiguracijom alata.

---

#### list

Navedite sve dostupne agente.

```bash
obelisk agent list
```

---

### auth

Naredba za upravljanje vjerodajnicama i prijavom za provajdere.

```bash
obelisk auth [command]
```

---

#### login

Obelisk pokreće lista provajdera na [Models.dev](https://models.dev), tako da možete koristiti `obelisk auth login` da konfigurirate API ključeve za bilo kojeg provajdera kojeg želite koristiti. Ovo je pohranjeno u `~/.local/share/obelisk/auth.json`.

```bash
obelisk auth login
```

Kada se Obelisk pokrene, učitava dobavljače iz datoteke vjerodajnica. I ako postoje neki ključevi definirani u vašim okruženjima ili `.env` fajl u vašem projektu.

---

#### list

Navodi sve autentifikovane dobavljače pohranjene u datoteci vjerodajnica.

```bash
obelisk auth list
```

Ili kratka verzija.

```bash
obelisk auth ls
```

---

#### logout

Odjavljuje vas s provajdera tako što ga briše iz datoteke vjerodajnica.

```bash
obelisk auth logout
```

---

### github

Upravljajte GitHub agentom za automatizaciju repozitorija.

```bash
obelisk github [command]
```

---

#### install

Instalirajte GitHub agenta u svoj repozitorij.

```bash
obelisk github install
```

Ovo postavlja neophodni tok rada GitHub Actions i vodi vas kroz proces konfiguracije. [Saznajte više](/docs/github).

---

#### run

Pokrenite GitHub agent. Ovo se obično koristi u GitHub Actions.

```bash
obelisk github run
```

##### Opcije

| Opcija                                | Opis                                   |
| ------------------------------------- | -------------------------------------- |
| <nobr><code>{"--event"}</code></nobr> | GitHub mock event za pokretanje agenta |
| <nobr><code>{"--token"}</code></nobr> | GitHub Personal Access Token           |

---

### mcp

Upravljajte Model Context Protocol (MCP) serverima.

```bash
obelisk mcp [command]
```

---

#### add

Dodajte MCP server svojoj konfiguraciji.

```bash
obelisk mcp add
```

Ova naredba će vas voditi kroz dodavanje lokalnog ili udaljenog MCP servera.

---

#### list

Navedite sve konfigurirane MCP servere i njihov status veze.

```bash
obelisk mcp list
```

Ili koristite kratku verziju.

```bash
obelisk mcp ls
```

---

#### auth

Autentifikujte se sa MCP serverom koji je omogućen za OAuth.

```bash
obelisk mcp auth [name]
```

Ako ne navedete ime servera, od vas će biti zatraženo da izaberete neki od dostupnih servera koji podržavaju OAuth.
Također možete navesti servere koji podržavaju OAuth i njihov status autentifikacije.

```bash
obelisk mcp auth list
```

Ili koristite kratku verziju.

```bash
obelisk mcp auth ls
```

---

#### logout

Uklonite OAuth vjerodajnice za MCP server.

```bash
obelisk mcp logout [name]
```

---

#### debug

Otklanjanje grešaka (debug) OAuth veze sa MCP serverom.

```bash
obelisk mcp debug <name>
```

---

### models

Navedite sve dostupne modele konfiguriranih provajdera.

```bash
obelisk models [provider]
```

Ova naredba prikazuje sve modele dostupne kod vaših konfiguriranih provajdera u formatu `provider/model`.
Ovo je korisno za pronalaženje tačnog naziva modela za korištenje u [vašoj konfiguraciji](/docs/config/).
Opciono možete proslijediti ID provajdera za filtriranje modela po tom dobavljaču.

```bash
obelisk models anthropic
```

#### Opcije

| Opcija                                  | Opis                                                                     |
| --------------------------------------- | ------------------------------------------------------------------------ |
| <nobr><code>{"--refresh"}</code></nobr> | Osvježite keš modela sa models.dev                                       |
| <nobr><code>{"--verbose"}</code></nobr> | Koristite detaljniji izlaz modela (uključuje metapodatke poput troškova) |

Koristite `--refresh` zastavicu da ažurirate keširanu listu modela. Ovo je korisno kada su novi modeli dodani provajderu i želite da ih vidite u Obelisk.

```bash
obelisk models --refresh
```

---

### run

Pokrenite Obelisk u neinteraktivnom modu tako što ćete direktno proslijediti prompt.

```bash
obelisk run [message..]
```

Ovo je korisno za skriptiranje, automatizaciju ili kada želite brz odgovor bez pokretanja punog TUI-ja. Na primjer:

```bash "obelisk run"
obelisk run Explain the use of context in Go
```

Također možete priključiti pokrenutu `obelisk serve` instancu kako biste izbjegli vrijeme hladnog pokretanja MCP servera pri svakom pokretanju:

```bash
# Start a headless server in one terminal
obelisk serve

# In another terminal, run commands that attach to it
obelisk run --attach http://localhost:4096 "Explain async/await in JavaScript"
```

#### Opcije

| Opcija                                   | Kratko | Opis                                                                                          |
| ---------------------------------------- | ------ | --------------------------------------------------------------------------------------------- |
| <nobr><code>{"--command"}</code></nobr>  |        | Naredba za pokretanje, koristite poruku za argumente                                          |
| <nobr><code>{"--continue"}</code></nobr> | `-c`   | Nastavite posljednju sesiju                                                                   |
| <nobr><code>{"--session"}</code></nobr>  | `-s`   | ID sesije za nastavak                                                                         |
| <nobr><code>{"--fork"}</code></nobr>     |        | Forkujte sesiju pri nastavku (koristiti sa `--continue` ili `--session`)                      |
| <nobr><code>{"--share"}</code></nobr>    |        | Podijelite sesiju                                                                             |
| <nobr><code>{"--model"}</code></nobr>    | `-m`   | Model za korištenje u obliku provider/model                                                   |
| <nobr><code>{"--agent"}</code></nobr>    |        | Agent za korištenje                                                                           |
| <nobr><code>{"--file"}</code></nobr>     | `-f`   | Fajlovi koje treba priložiti poruci                                                           |
| <nobr><code>{"--format"}</code></nobr>   |        | Format: default (formatiran) ili json (sirovi JSON događaji)                                  |
| <nobr><code>{"--title"}</code></nobr>    |        | Naslov sesije (koristi skraćeni prompt ako nije navedena vrijednost)                          |
| <nobr><code>{"--attach"}</code></nobr>   |        | Priključite na pokrenuti Obelisk server (npr. http://localhost:4096)                         |
| <nobr><code>{"--password"}</code></nobr> | `-p`   | Lozinka za osnovnu autentifikaciju (zadano: `OBELISK_SERVER_PASSWORD`)                       |
| <nobr><code>{"--username"}</code></nobr> | `-u`   | Korisničko ime za osnovnu autentifikaciju (zadano: `OBELISK_SERVER_USERNAME` ili `obelisk`) |
| <nobr><code>{"--dir"}</code></nobr>      |        | Direktorij za pokretanje, ili putanja na udaljenom serveru pri spajanju                       |
| <nobr><code>{"--variant"}</code></nobr>  |        | Varijanta modela (napor zaključivanja specifičan za provajdera)                               |
| <nobr><code>{"--thinking"}</code></nobr> |        | Prikaži blokove razmišljanja                                                                  |
| <nobr><code>{"--port"}</code></nobr>     |        | Port za lokalni server (zadano na nasumični port)                                             |

---

### serve

Pokrenite Obelisk headless server za API pristup. Pogledajte [server docs](/docs/server) za kompletan HTTP interfejs.

```bash
obelisk serve
```

Ovo pokreće HTTP server koji pruža API pristup funkcionalnosti Obelisk-a bez TUI interfejsa. Postavite `OBELISK_SERVER_PASSWORD` da omogućite HTTP osnovnu auth (korisničko ime je zadano na `obelisk`).

#### Opcije

| Opcija                                   | Opis                                                  |
| ---------------------------------------- | ----------------------------------------------------- |
| <nobr><code>{"--port"}</code></nobr>     | Port na kojem treba slušati                           |
| <nobr><code>{"--hostname"}</code></nobr> | Hostname na kojem treba slušati                       |
| <nobr><code>{"--mdns"}</code></nobr>     | Omogući mDNS otkrivanje                               |
| <nobr><code>{"--cors"}</code></nobr>     | Dodatni origin(i) pretraživača koji dozvoljavaju CORS |

---

### session

Upravljajte Obelisk sesijama.

```bash
obelisk session [command]
```

---

#### list

Navedite sve Obelisk sesije.

```bash
obelisk session list
```

##### Opcije

| Opcija                                    | Kratko | Opis                                   |
| ----------------------------------------- | ------ | -------------------------------------- |
| <nobr><code>{"--max-count"}</code></nobr> | `-n`   | Ograničenje na N najnovijih sesija     |
| <nobr><code>{"--format"}</code></nobr>    |        | Izlazni format: table ili json (table) |

---

### stats

Prikaži statistiku upotrebe tokena i troškova za vaše Obelisk sesije.

```bash
obelisk stats
```

#### Opcije

| Opcija                                  | Opis                                                                                                       |
| --------------------------------------- | ---------------------------------------------------------------------------------------------------------- |
| <nobr><code>{"--days"}</code></nobr>    | Prikaži statistiku za zadnjih N dana (sva vremena)                                                         |
| <nobr><code>{"--tools"}</code></nobr>   | Broj alata za prikaz (svi)                                                                                 |
| <nobr><code>{"--models"}</code></nobr>  | Prikaži raščlambu korištenja modela (skriveno prema zadanim postavkama). Proslijedite broj za prikaz top N |
| <nobr><code>{"--project"}</code></nobr> | Filtriraj po projektu (svi projekti, prazan niz: trenutni projekt)                                         |

---

### export

Izvezite podatke sesije kao JSON.

```bash
obelisk export [sessionID]
```

Ako ne unesete ID sesije, od vas će biti zatraženo da odaberete neku od dostupnih sesija.

---

### import

Uvezite podatke sesije iz JSON datoteke ili Obelisk dijeljenog URL-a.

```bash
obelisk import <file>
```

Možete uvesti iz lokalne datoteke ili Obelisk dijeljenog URL-a.

```bash
obelisk import session.json
obelisk import https://opncd.ai/s/abc123
```

---

### web

Pokrenite Obelisk headless server sa web interfejsom.

```bash
obelisk web
```

Ovo pokreće HTTP server i otvara web pretraživač za pristup Obelisk-u preko web interfejsa. Postavite `OBELISK_SERVER_PASSWORD` da omogućite HTTP osnovnu auth (korisničko ime je zadano na `obelisk`).

#### Opcije

| Opcija                                   | Opis                                                  |
| ---------------------------------------- | ----------------------------------------------------- |
| <nobr><code>{"--port"}</code></nobr>     | Port na kojem treba slušati                           |
| <nobr><code>{"--hostname"}</code></nobr> | Hostname na kojem treba slušati                       |
| <nobr><code>{"--mdns"}</code></nobr>     | Omogući mDNS otkrivanje                               |
| <nobr><code>{"--cors"}</code></nobr>     | Dodatni origin(i) pretraživača koji dozvoljavaju CORS |

---

### acp

Pokrenite ACP (Agent Client Protocol) server.

```bash
obelisk acp
```

Ova naredba pokreće ACP server koji komunicira preko stdin/stdout koristeći nd-JSON.

#### Opcije

| Opcija                                   | Opis                        |
| ---------------------------------------- | --------------------------- |
| <nobr><code>{"--cwd"}</code></nobr>      | Radni direktorij            |
| <nobr><code>{"--port"}</code></nobr>     | Port na kojem treba slušati |
| <nobr><code>{"--hostname"}</code></nobr> | Hostname na kojem slušati   |

---

### uninstall

Deinstalirajte Obelisk i uklonite sve povezane datoteke.

```bash
obelisk uninstall
```

#### Opcije

| Opcija                                      | Kratko | Opis                                          |
| ------------------------------------------- | ------ | --------------------------------------------- |
| <nobr><code>{"--keep-config"}</code></nobr> | `-c`   | Sačuvajte konfiguracijske datoteke            |
| <nobr><code>{"--keep-data"}</code></nobr>   | `-d`   | Sačuvajte podatke i snimke sesije             |
| <nobr><code>{"--dry-run"}</code></nobr>     |        | Pokažite šta bi bilo uklonjeno bez uklanjanja |
| <nobr><code>{"--force"}</code></nobr>       | `-f`   | Preskoči upite za potvrdu                     |

---

### upgrade

Ažurira Obelisk na najnoviju verziju ili određenu verziju.

```bash
obelisk upgrade [target]
```

Za nadogradnju na najnoviju verziju.

```bash
obelisk upgrade
```

Za nadogradnju na određenu verziju.

```bash
obelisk upgrade v0.1.48
```

#### Opcije

| Opcija                                 | Kratko | Opis                                                    |
| -------------------------------------- | ------ | ------------------------------------------------------- |
| <nobr><code>{"--method"}</code></nobr> | `-m`   | Korišteni način instalacije; curl, npm, pnpm, bun, brew |

---

## Globalne opcije

Obelisk CLI prihvata sljedeće globalne zastavice.

| Opcija                                     | Kratko | Opis                                      |
| ------------------------------------------ | ------ | ----------------------------------------- |
| <nobr><code>{"--help"}</code></nobr>       | `-h`   | Prikaži pomoć                             |
| <nobr><code>{"--version"}</code></nobr>    | `-v`   | Ispiši broj verzije                       |
| <nobr><code>{"--print-logs"}</code></nobr> |        | Ispis logova u stderr                     |
| <nobr><code>{"--log-level"}</code></nobr>  |        | Nivo logovanja (DEBUG, INFO, WARN, ERROR) |

---

## Varijable okruženja

Obelisk se može konfigurirati pomoću varijabli okruženja.

| Varijabla                             | Tip     | Opis                                                               |
| ------------------------------------- | ------- | ------------------------------------------------------------------ |
| `OBELISK_AUTO_SHARE`                 | boolean | Automatski dijeli sesije                                           |
| `OBELISK_GIT_BASH_PATH`              | string  | Putanja do Git Bash izvršne datoteke na Windows-u                  |
| `OBELISK_CONFIG`                     | string  | Putanja do konfiguracijskog fajla                                  |
| `OBELISK_TUI_CONFIG`                 | string  | Putanja do TUI konfiguracijskog fajla                              |
| `OBELISK_CONFIG_DIR`                 | string  | Putanja do konfiguracijskog direktorija                            |
| `OBELISK_CONFIG_CONTENT`             | string  | Inline json konfiguracijski sadržaj                                |
| `OBELISK_DISABLE_AUTOUPDATE`         | boolean | Onemogući automatske provjere ažuriranja                           |
| `OBELISK_DISABLE_PRUNE`              | boolean | Onemogući brisanje (pruning) starih podataka                       |
| `OBELISK_DISABLE_TERMINAL_TITLE`     | boolean | Onemogući automatsko ažuriranje naslova terminala                  |
| `OBELISK_PERMISSION`                 | string  | Inline json konfiguracija dozvola                                  |
| `OBELISK_DISABLE_DEFAULT_PLUGINS`    | boolean | Onemogući podrazumijevane dodatke (plugins)                        |
| `OBELISK_DISABLE_LSP_DOWNLOAD`       | boolean | Onemogući automatsko preuzimanje LSP servera                       |
| `OBELISK_ENABLE_EXPERIMENTAL_MODELS` | boolean | Omogući eksperimentalne modele                                     |
| `OBELISK_DISABLE_AUTOCOMPACT`        | boolean | Onemogući automatsko sažimanje konteksta                           |
| `OBELISK_DISABLE_CLAUDE_CODE`        | boolean | Onemogući čitanje iz `.claude` (prompt + vještine)                 |
| `OBELISK_DISABLE_CLAUDE_CODE_PROMPT` | boolean | Onemogući čitanje `~/.claude/CLAUDE.md`                            |
| `OBELISK_DISABLE_CLAUDE_CODE_SKILLS` | boolean | Onemogući učitavanje `.claude/skills`                              |
| `OBELISK_DISABLE_MODELS_FETCH`       | boolean | Onemogući dohvaćanje modela iz udaljenih izvora                    |
| `OBELISK_FAKE_VCS`                   | string  | Lažni VCS provajder za potrebe testiranja                          |
| `OBELISK_CLIENT`                     | string  | Identifikator klijenta (zadano na `cli`)                           |
| `OBELISK_ENABLE_EXA`                 | boolean | Omogući Exa alate za web pretraživanje                             |
| `OBELISK_SERVER_PASSWORD`            | string  | Omogući osnovnu autentifikaciju za `serve`/`web`                   |
| `OBELISK_SERVER_USERNAME`            | string  | Poništi osnovno korisničko ime autentifikacije (zadano `obelisk`) |
| `OBELISK_MODELS_URL`                 | string  | Prilagođeni URL za dohvaćanje konfiguracije modela                 |

---

### Eksperimentalno

Ove varijable okruženja omogućavaju eksperimentalne karakteristike koje se mogu promijeniti ili ukloniti.

| Varijabla                                       | Tip     | Opis                                                    |
| ----------------------------------------------- | ------- | ------------------------------------------------------- |
| `OBELISK_EXPERIMENTAL`                         | boolean | Omogući eksperimentalne funkcije pod zbirnom zastavicom |
| `OBELISK_EXPERIMENTAL_ICON_DISCOVERY`          | boolean | Omogući otkrivanje ikona                                |
| `OBELISK_EXPERIMENTAL_DISABLE_COPY_ON_SELECT`  | boolean | Onemogući kopiranje pri odabiru u TUI                   |
| `OBELISK_EXPERIMENTAL_BASH_DEFAULT_TIMEOUT_MS` | number  | Zadano vremensko ograničenje za bash naredbe u ms       |
| `OBELISK_EXPERIMENTAL_OUTPUT_TOKEN_MAX`        | number  | Maksimalni izlazni tokeni za LLM odgovore               |
| `OBELISK_EXPERIMENTAL_FILEWATCHER`             | boolean | Omogući praćenje datoteka za cijeli direktorij          |
| `OBELISK_EXPERIMENTAL_OXFMT`                   | boolean | Omogući oxfmt formatter                                 |
| `OBELISK_EXPERIMENTAL_LSP_TOOL`                | boolean | Omogući eksperimentalni LSP alat                        |
| `OBELISK_EXPERIMENTAL_DISABLE_FILEWATCHER`     | boolean | Onemogući praćenje datoteka                             |
| `OBELISK_EXPERIMENTAL_EXA`                     | boolean | Omogući eksperimentalne Exa funkcije                    |
| `OBELISK_EXPERIMENTAL_LSP_TY`                  | boolean | Omogući TY LSP za python datoteke                       |
| `OBELISK_EXPERIMENTAL_PLAN_MODE`               | boolean | Omogući Plan mod                                        |
| `OBELISK_EXPERIMENTAL_BACKGROUND_SUBAGENTS`    | boolean | Omogući pozadinske zadatke subagenata                   |
| `OBELISK_EXPERIMENTAL_EVENT_SYSTEM`            | boolean | Omogući eksperimentalni sistem događaja                 |
| `OBELISK_EXPERIMENTAL_NATIVE_LLM`              | boolean | Omogući nativnu putanju LLM zahtjeva                    |
| `OBELISK_EXPERIMENTAL_PARALLEL`                | boolean | Omogući paralelno izvršavanje web pretrage              |
| `OBELISK_EXPERIMENTAL_SCOUT`                   | boolean | Omogući Scout subagenta                                 |
| `OBELISK_EXPERIMENTAL_WORKSPACES`              | boolean | Omogući podršku za radne prostore                       |
