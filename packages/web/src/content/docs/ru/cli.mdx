---
title: CLI
description: Параметры и команда obelisk CLI.
---

import { Tabs, TabItem } from "@astrojs/starlight/components"

CLI obelisk по умолчанию запускает [TUI](/docs/tui) при запуске без каких-либо аргументов.

```bash
obelisk
```

Но он также принимает команды, описанные на этой странице. Это позволяет вам программно взаимодействовать с obelisk.

```bash
obelisk run "Explain how closures work in JavaScript"
```

---

### tui

Запустите TUI obelisk.

```bash
obelisk [project]
```

#### Флаги

| Флаг                                     | Короткий | Описание                                                                      |
| ---------------------------------------- | -------- | ----------------------------------------------------------------------------- |
| <nobr><code>{"--continue"}</code></nobr> | `-c`     | Продолжить последний сеанс                                                    |
| <nobr><code>{"--session"}</code></nobr>  | `-s`     | Идентификатор сеанса для продолжения                                          |
| <nobr><code>{"--fork"}</code></nobr>     |          | Разветвить сеанс при продолжении (используйте с `--continue` или `--session`) |
| <nobr><code>{"--prompt"}</code></nobr>   |          | Промпт для использования                                                      |
| <nobr><code>{"--model"}</code></nobr>    | `-m`     | Модель для использования в виде поставщика/модели.                            |
| <nobr><code>{"--agent"}</code></nobr>    |          | Агент для использования                                                       |
| <nobr><code>{"--port"}</code></nobr>     |          | Порт для прослушивания                                                        |
| <nobr><code>{"--hostname"}</code></nobr> |          | Имя хоста для прослушивания                                                   |

---

## Команды

CLI obelisk также имеет следующие команды.

---

### agent

Управляйте агентами для obelisk.

```bash
obelisk agent [command]
```

---

### attach

Подключите терминал к уже работающему внутреннему серверу obelisk, запущенному с помощью команд `serve` или `web`.

```bash
obelisk attach [url]
```

Это позволяет использовать TUI с удаленным сервером obelisk. Например:

```bash
# Start the backend server for web/mobile access
obelisk web --port 4096 --hostname 0.0.0.0

# In another terminal, attach the TUI to the running backend
obelisk attach http://10.20.30.40:4096
```

#### Флаги

| Флаг                                     | Короткий | Описание                                                                                         |
| ---------------------------------------- | -------- | ------------------------------------------------------------------------------------------------ |
| <nobr><code>{"--dir"}</code></nobr>      |          | Рабочий каталог для запуска TUI                                                                  |
| <nobr><code>{"--continue"}</code></nobr> | `-c`     | Продолжить последний сеанс                                                                       |
| <nobr><code>{"--session"}</code></nobr>  | `-s`     | Идентификатор сеанса для продолжения                                                             |
| <nobr><code>{"--fork"}</code></nobr>     |          | Создать ответвление сеанса при продолжении (используйте с `--continue` или `--session`)          |
| <nobr><code>{"--password"}</code></nobr> | `-p`     | Пароль базовой аутентификации (по умолчанию `OBELISK_SERVER_PASSWORD`)                          |
| <nobr><code>{"--username"}</code></nobr> | `-u`     | Имя пользователя базовой аутентификации (по умолчанию `OBELISK_SERVER_USERNAME` или `obelisk`) |

---

#### create

Создайте нового агента с пользовательской конфигурацией.

```bash
obelisk agent create
```

Эта команда поможет вам создать новый агент с настраиваемой системной подсказкой и настройкой инструмента.

---

#### list

Перечислите всех доступных агентов.

```bash
obelisk agent list
```

---

### auth

Команда для управления учетными данными и входом в систему для провайдеров.

```bash
obelisk auth [command]
```

---

#### login

obelisk использует список провайдеров с [Models.dev](https://models.dev), поэтому вы можете использовать `obelisk auth login` для настройки ключей API для любого поставщика, которого вы хотите использовать. Это хранится в `~/.local/share/obelisk/auth.json`.

```bash
obelisk auth login
```

Когда obelisk запускается, он загружает поставщиков из файла учетных данных. И если в ваших средах определены какие-либо ключи или файл `.env` в вашем проекте.

---

#### list

Перечисляет всех проверенных поставщиков, которые хранятся в файле учетных данных.

```bash
obelisk auth list
```

Или короткая версия.

```bash
obelisk auth ls
```

---

#### logout

Выключает вас из провайдера, удаляя его из файла учетных данных.

```bash
obelisk auth logout
```

---

### github

Управляйте агентом GitHub для автоматизации репозитория.

```bash
obelisk github [command]
```

---

#### install

Установите агент GitHub в свой репозиторий.

```bash
obelisk github install
```

Это настроит необходимый рабочий процесс GitHub Actions и проведет вас через процесс настройки. [Подробнее](/docs/github).

---

#### run

Запустите агент GitHub. Обычно это используется в действиях GitHub.

```bash
obelisk github run
```

##### Флаги

| Флаг                                  | Описание                                      |
| ------------------------------------- | --------------------------------------------- |
| <nobr><code>{"--event"}</code></nobr> | Имитирующее событие GitHub для запуска агента |
| <nobr><code>{"--token"}</code></nobr> | Токен личного доступа GitHub                  |

---

### mcp

Управляйте серверами протокола контекста модели.

```bash
obelisk mcp [command]
```

---

#### add

Добавьте сервер MCP в свою конфигурацию.

```bash
obelisk mcp add
```

Эта команда поможет вам добавить локальный или удаленный сервер MCP.

---

#### list

Перечислите все настроенные серверы MCP и состояние их подключения.

```bash
obelisk mcp list
```

Или используйте короткую версию.

```bash
obelisk mcp ls
```

---

#### auth

Аутентификация с помощью сервера MCP с поддержкой OAuth.

```bash
obelisk mcp auth [name]
```

Если вы не укажете имя сервера, вам будет предложено выбрать один из доступных серверов с поддержкой OAuth.

Вы также можете перечислить серверы с поддержкой OAuth и их статус аутентификации.

```bash
obelisk mcp auth list
```

Или используйте короткую версию.

```bash
obelisk mcp auth ls
```

---

#### logout

Удалите учетные данные OAuth для сервера MCP.

```bash
obelisk mcp logout [name]
```

---

#### debug

Отладка проблем с подключением OAuth для сервера MCP.

```bash
obelisk mcp debug <name>
```

---

### models

Перечислите все доступные модели от настроенных поставщиков.

```bash
obelisk models [provider]
```

Эта команда отображает все модели, доступные у настроенных вами поставщиков, в формате `provider/model`.

Это полезно для определения точного названия модели, которое будет использоваться в [вашем config](/docs/config/).

При желании вы можете передать идентификатор поставщика, чтобы фильтровать модели по этому поставщику.

```bash
obelisk models anthropic
```

#### Флаги

| Флаг                                    | Описание                                                                          |
| --------------------------------------- | --------------------------------------------------------------------------------- |
| <nobr><code>{"--refresh"}</code></nobr> | Обновите кеш моделей на сайте models.dev.                                         |
| <nobr><code>{"--verbose"}</code></nobr> | Используйте более подробный вывод модели (включая метаданные, такие как затраты). |

Используйте флаг `--refresh` для обновления списка кэшированных моделей. Это полезно, когда к поставщику добавлены новые модели и вы хотите увидеть их в obelisk.

```bash
obelisk models --refresh
```

---

### run

Запустите obelisk в неинтерактивном режиме, передав приглашение напрямую.

```bash
obelisk run [message..]
```

Это полезно для создания сценариев, автоматизации или когда вам нужен быстрый ответ без запуска полного TUI. Например.

```bash "obelisk run"
obelisk run Explain the use of context in Go
```

Вы также можете подключиться к работающему экземпляру `obelisk serve`, чтобы избежать холодной загрузки сервера MCP при каждом запуске:

```bash
# Start a headless server in one terminal
obelisk serve

# In another terminal, run commands that attach to it
obelisk run --attach http://localhost:4096 "Explain async/await in JavaScript"
```

#### Флаги

| Флаг                                     | Короткий | Описание                                                                                         |
| ---------------------------------------- | -------- | ------------------------------------------------------------------------------------------------ |
| <nobr><code>{"--command"}</code></nobr>  |          | Команда для запуска, используйте сообщение для аргументов                                        |
| <nobr><code>{"--continue"}</code></nobr> | `-c`     | Продолжить последний сеанс                                                                       |
| <nobr><code>{"--session"}</code></nobr>  | `-s`     | Идентификатор сеанса для продолжения                                                             |
| <nobr><code>{"--fork"}</code></nobr>     |          | Разветвить сеанс при продолжении (используйте с `--continue` или `--session`)                    |
| <nobr><code>{"--share"}</code></nobr>    |          | Поделиться сеансом                                                                               |
| <nobr><code>{"--model"}</code></nobr>    | `-m`     | Модель для использования в виде поставщика/модели.                                               |
| <nobr><code>{"--agent"}</code></nobr>    |          | Агент для использования                                                                          |
| <nobr><code>{"--file"}</code></nobr>     | `-f`     | Файл(ы) для прикрепления к сообщению                                                             |
| <nobr><code>{"--format"}</code></nobr>   |          | Формат: по умолчанию (отформатированный) или json (необработанные события JSON).                 |
| <nobr><code>{"--title"}</code></nobr>    |          | Название сеанса (использует усеченное приглашение, если значение не указано)                     |
| <nobr><code>{"--attach"}</code></nobr>   |          | Подключитесь к работающему серверу obelisk (например, http://localhost:4096)                    |
| <nobr><code>{"--password"}</code></nobr> | `-p`     | Пароль базовой аутентификации (по умолчанию `OBELISK_SERVER_PASSWORD`)                          |
| <nobr><code>{"--username"}</code></nobr> | `-u`     | Имя пользователя базовой аутентификации (по умолчанию `OBELISK_SERVER_USERNAME` или `obelisk`) |
| <nobr><code>{"--dir"}</code></nobr>      |          | Каталог для выполнения или путь на удалённом сервере при подключении                             |
| <nobr><code>{"--variant"}</code></nobr>  |          | Вариант модели (уровень рассуждений для провайдера)                                              |
| <nobr><code>{"--thinking"}</code></nobr> |          | Показать блоки размышлений                                                                       |
| <nobr><code>{"--port"}</code></nobr>     |          | Порт локального сервера (по умолчанию случайный порт)                                            |

---

### serve

Запустите автономный сервер obelisk для доступа к API. Полный HTTP-интерфейс можно найти в [server docs](/docs/server).

```bash
obelisk serve
```

При этом запускается HTTP-сервер, который обеспечивает доступ API к функциям obelisk без интерфейса TUI. Установите `OBELISK_SERVER_PASSWORD`, чтобы включить базовую аутентификацию HTTP (имя пользователя по умолчанию — `obelisk`).

#### Флаги

| Флаг                                     | Описание                                                      |
| ---------------------------------------- | ------------------------------------------------------------- |
| <nobr><code>{"--port"}</code></nobr>     | Порт для прослушивания                                        |
| <nobr><code>{"--hostname"}</code></nobr> | Имя хоста для прослушивания                                   |
| <nobr><code>{"--mdns"}</code></nobr>     | Включить обнаружение mDNS                                     |
| <nobr><code>{"--cors"}</code></nobr>     | Дополнительные источники браузера, позволяющие разрешить CORS |

---

### session

Управляйте сессиями obelisk.

```bash
obelisk session [command]
```

---

#### list

Перечислите все сеансы obelisk.

```bash
obelisk session list
```

##### Флаги

| Флаг                                      | Короткий | Описание                                  |
| ----------------------------------------- | -------- | ----------------------------------------- |
| <nobr><code>{"--max-count"}</code></nobr> | `-n`     | Ограничить N последних сеансов.           |
| <nobr><code>{"--format"}</code></nobr>    |          | Формат вывода: таблица или json (таблица) |

---

### stats

Покажите статистику использования токенов и затрат для ваших сеансов obelisk.

```bash
obelisk stats
```

#### Флаги

| Флаг                                    | Описание                                                                                                   |
| --------------------------------------- | ---------------------------------------------------------------------------------------------------------- |
| <nobr><code>{"--days"}</code></nobr>    | Показать статистику за последние N дней (все время)                                                        |
| <nobr><code>{"--tools"}</code></nobr>   | Количество инструментов для отображения (все)                                                              |
| <nobr><code>{"--models"}</code></nobr>  | Показать разбивку по использованию модели (по умолчанию скрыто). Передайте номер, чтобы показать верхнюю N |
| <nobr><code>{"--project"}</code></nobr> | Фильтровать по проекту (все проекты, пустая строка: текущий проект)                                        |

---

### export

Экспортируйте данные сеанса в формате JSON.

```bash
obelisk export [sessionID]
```

Если вы не укажете идентификатор сеанса, вам будет предложено выбрать один из доступных сеансов.

---

### import

Импортируйте данные сеанса из файла JSON или URL-адреса общего ресурса obelisk.

```bash
obelisk import <file>
```

Вы можете импортировать из локального файла или URL-адреса общего ресурса obelisk.

```bash
obelisk import session.json
obelisk import https://opncd.ai/s/abc123
```

---

### web

Запустите автономный сервер obelisk с веб-интерфейсом.

```bash
obelisk web
```

При этом запускается HTTP-сервер и открывается веб-браузер для доступа к obelisk через веб-интерфейс. Установите `OBELISK_SERVER_PASSWORD`, чтобы включить базовую аутентификацию HTTP (имя пользователя по умолчанию — `obelisk`).

#### Флаги

| Флаг                                     | Описание                                                      |
| ---------------------------------------- | ------------------------------------------------------------- |
| <nobr><code>{"--port"}</code></nobr>     | Порт для прослушивания                                        |
| <nobr><code>{"--hostname"}</code></nobr> | Имя хоста для прослушивания                                   |
| <nobr><code>{"--mdns"}</code></nobr>     | Включить обнаружение mDNS                                     |
| <nobr><code>{"--cors"}</code></nobr>     | Дополнительные источники браузера, позволяющие разрешить CORS |

---

### acp

Запустите сервер ACP (агент-клиентский протокол).

```bash
obelisk acp
```

Эта команда запускает сервер ACP, который обменивается данными через stdin/stdout с использованием nd-JSON.

#### Флаги

| Флаг                                     | Описание                    |
| ---------------------------------------- | --------------------------- |
| <nobr><code>{"--cwd"}</code></nobr>      | Рабочий каталог             |
| <nobr><code>{"--port"}</code></nobr>     | Порт для прослушивания      |
| <nobr><code>{"--hostname"}</code></nobr> | Имя хоста для прослушивания |

---

### uninstall

Удалите obelisk и удалите все связанные файлы.

```bash
obelisk uninstall
```

#### Флаги

| Флаг                                        | Короткий | Описание                                   |
| ------------------------------------------- | -------- | ------------------------------------------ |
| <nobr><code>{"--keep-config"}</code></nobr> | `-c`     | Сохраняйте файлы конфигурации              |
| <nobr><code>{"--keep-data"}</code></nobr>   | `-d`     | Храните данные сеанса и снимки             |
| <nobr><code>{"--dry-run"}</code></nobr>     |          | Покажите, что было бы удалено без удаления |
| <nobr><code>{"--force"}</code></nobr>       | `-f`     | Пропустить запросы подтверждения           |

---

### upgrade

Обновляет obelisk до последней версии или определенной версии.

```bash
obelisk upgrade [target]
```

Чтобы обновиться до последней версии.

```bash
obelisk upgrade
```

Для обновления до определенной версии.

```bash
obelisk upgrade v0.1.48
```

#### Флаги

| Флаг                                   | Короткий | Описание                                                  |
| -------------------------------------- | -------- | --------------------------------------------------------- |
| <nobr><code>{"--method"}</code></nobr> | `-m`     | Используемый метод установки: local, npm, pnpm, bun, brew |

---

## Глобальные флаги

CLI obelisk принимает следующие глобальные флаги.

| Флаг                                       | Короткий | Описание                                   |
| ------------------------------------------ | -------- | ------------------------------------------ |
| <nobr><code>{"--help"}</code></nobr>       | `-h`     | Отобразить справку                         |
| <nobr><code>{"--version"}</code></nobr>    | `-v`     | Распечатать номер версии                   |
| <nobr><code>{"--print-logs"}</code></nobr> |          | Печать журналов в stderr                   |
| <nobr><code>{"--log-level"}</code></nobr>  |          | Уровень журнала (DEBUG, INFO, WARN, ERROR) |

---

## Переменные среды

obelisk можно настроить с помощью переменных среды.

| Переменная                            | Тип                 | Описание                                                                         |
| ------------------------------------- | ------------------- | -------------------------------------------------------------------------------- |
| `OBELISK_AUTO_SHARE`                 | логическое значение | Автоматически делиться сеансами                                                  |
| `OBELISK_GIT_BASH_PATH`              | строка              | Путь к исполняемому файлу Git Bash в Windows                                     |
| `OBELISK_CONFIG`                     | строка              | Путь к файлу конфигурации                                                        |
| `OBELISK_TUI_CONFIG`                 | строка              | Путь к файлу конфигурации TUI                                                    |
| `OBELISK_CONFIG_DIR`                 | строка              | Путь к каталогу конфигурации                                                     |
| `OBELISK_CONFIG_CONTENT`             | строка              | Встроенное содержимое конфигурации json                                          |
| `OBELISK_DISABLE_AUTOUPDATE`         | логическое значение | Отключить автоматическую проверку обновлений                                     |
| `OBELISK_DISABLE_PRUNE`              | логическое значение | Отключить удаление старых данных                                                 |
| `OBELISK_DISABLE_TERMINAL_TITLE`     | логическое значение | Отключить автоматическое обновление заголовка терминала                          |
| `OBELISK_PERMISSION`                 | строка              | Встроенная конфигурация разрешений json                                          |
| `OBELISK_DISABLE_DEFAULT_PLUGINS`    | логическое значение | Отключить плагины по умолчанию                                                   |
| `OBELISK_DISABLE_LSP_DOWNLOAD`       | логическое значение | Отключить автоматическую загрузку LSP-сервера                                    |
| `OBELISK_ENABLE_EXPERIMENTAL_MODELS` | логическое значение | Включить экспериментальные модели                                                |
| `OBELISK_DISABLE_AUTOCOMPACT`        | логическое значение | Отключить автоматическое сжатие контекста                                        |
| `OBELISK_DISABLE_CLAUDE_CODE`        | логическое значение | Отключить чтение из `.claude` (подсказка + навыки)                               |
| `OBELISK_DISABLE_CLAUDE_CODE_PROMPT` | логическое значение | Отключить чтение `~/.claude/CLAUDE.md`                                           |
| `OBELISK_DISABLE_CLAUDE_CODE_SKILLS` | логическое значение | Отключить загрузку `.claude/skills`                                              |
| `OBELISK_DISABLE_MODELS_FETCH`       | логическое значение | Отключить получение моделей из удаленных источников                              |
| `OBELISK_FAKE_VCS`                   | строка              | Поддельный поставщик VCS для целей тестирования                                  |
| `OBELISK_CLIENT`                     | строка              | Идентификатор клиента (по умолчанию `cli`)                                       |
| `OBELISK_ENABLE_EXA`                 | логическое значение | Включить инструменты веб-поиска Exa                                              |
| `OBELISK_SERVER_PASSWORD`            | строка              | Включить базовую аутентификацию для `serve`/`web`                                |
| `OBELISK_SERVER_USERNAME`            | строка              | Переопределить имя пользователя базовой аутентификации (по умолчанию `obelisk`) |
| `OBELISK_MODELS_URL`                 | строка              | Пользовательский URL-адрес для получения конфигурации модели                     |

---

### Экспериментальные функции

Эти переменные среды позволяют использовать экспериментальные функции, которые могут быть изменены или удалены.

| Переменная                                      | Тип                 | Описание                                               |
| ----------------------------------------------- | ------------------- | ------------------------------------------------------ |
| `OBELISK_EXPERIMENTAL`                         | логическое значение | Включить экспериментальные функции под общим флагом    |
| `OBELISK_EXPERIMENTAL_ICON_DISCOVERY`          | логическое значение | Включить обнаружение значков                           |
| `OBELISK_EXPERIMENTAL_DISABLE_COPY_ON_SELECT`  | логическое значение | Отключить копирование при выборе в TUI                 |
| `OBELISK_EXPERIMENTAL_BASH_DEFAULT_TIMEOUT_MS` | число               | Таймаут по умолчанию для команд bash в мс              |
| `OBELISK_EXPERIMENTAL_OUTPUT_TOKEN_MAX`        | число               | Максимальное количество токенов вывода для ответов LLM |
| `OBELISK_EXPERIMENTAL_FILEWATCHER`             | логическое значение | Включить просмотр файлов для всего каталога            |
| `OBELISK_EXPERIMENTAL_OXFMT`                   | логическое значение | Включить форматтер oxfmt                               |
| `OBELISK_EXPERIMENTAL_LSP_TOOL`                | логическое значение | Включить экспериментальный инструмент LSP              |
| `OBELISK_EXPERIMENTAL_DISABLE_FILEWATCHER`     | логическое значение | Отключить просмотрщик файлов                           |
| `OBELISK_EXPERIMENTAL_EXA`                     | логическое значение | Включить экспериментальные функции Exa                 |
| `OBELISK_EXPERIMENTAL_LSP_TY`                  | логическое значение | Включить TY LSP для файлов python                      |
| `OBELISK_EXPERIMENTAL_PLAN_MODE`               | логическое значение | Включить режим плана                                   |
| `OBELISK_EXPERIMENTAL_BACKGROUND_SUBAGENTS`    | логическое значение | Включить фоновые задачи субагентов                     |
| `OBELISK_EXPERIMENTAL_EVENT_SYSTEM`            | логическое значение | Включить экспериментальную систему событий             |
| `OBELISK_EXPERIMENTAL_NATIVE_LLM`              | логическое значение | Включить нативный путь запросов LLM                    |
| `OBELISK_EXPERIMENTAL_PARALLEL`                | логическое значение | Включить параллельное выполнение веб-поиска            |
| `OBELISK_EXPERIMENTAL_SCOUT`                   | логическое значение | Включить субагент Scout                                |
| `OBELISK_EXPERIMENTAL_WORKSPACES`              | логическое значение | Включить поддержку рабочих областей                    |
